import React from 'react';

const ExperienceEntryDisplay = ({ title }) => (
  <nav className="panel">
    <p className="panel-heading">{title}</p>

    <div className="panel-block">
      title
    </div>
  </nav>
)

export default ExperienceEntryDisplay;
