{% extends 'base.html.twig' %}

{% block body %}

<h2>Users</h2>

<ul>
  {% for user in users %}
  <li>
    <a href="{{ path('user_show', {'id': user.id}) }}">
      {{ user.username }}
    </a>
  </li>
  {% endfor %}
</ul>

{% endblock %}
