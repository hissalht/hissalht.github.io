{% extends 'base.html.twig' %}


{% block body %}
<ul>
    {% for user in users %}
        <li>
            {# <a href="{{ path('user_show', {'id': user.id}) }}"> #}
            <a href="#">
                {{ user.name }}
            </a>
        </li>
    {% endfor %}
</ul>
{% endblock %}
