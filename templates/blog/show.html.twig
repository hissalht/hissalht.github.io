{% extends 'base.html.twig' %}

{% block body %}

<p>ID : {{ post.id }}</p>

<p>Title : {{ post.title }}</p>

<p>Content : {{ post.content }}</p>

{# <p>{{ post.publicationDate }}</p>

<p>{{ post.editDate }}</p> #}

{% endblock %}